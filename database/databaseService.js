// Database service that works in Electron main process
// All operations are now handled through IPC in main process

// Export empty object since all operations are handled through IPC
module.exports = {};
